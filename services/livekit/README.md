# Intro
a framework for create conference call, meetings...

# Note
livekit support configuring using environment variable, find all available option use(this may not work if your livekit version is [too old](https://github.com/livekit/livekit/issues/4262)):
```bash
docker run --rm -it livekit/livekit-server help-verbose
```

# Status
not enough tested but seems ok

