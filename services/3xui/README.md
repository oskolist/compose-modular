# Status
stable but 3x-ui does not allow configuration set using config file at all and thus is not ideal for automating the configuring of inbound/outbound which requires respective nginx config

# TODO
- configure the nginx to host 3x-ui under a subpath in main_domain instead of new subdomain
- looking for better alternate
